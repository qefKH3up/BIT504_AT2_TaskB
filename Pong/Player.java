
// Enum (enumerator) means "specifically listed" and is used for constants (unchangeable variables).
public enum Player {
	// creating Player.one and Player.two instead of 1 and 2 makes the difference clearer.
	One, Two
}
